{
  "name": "sleepness",
  "version": "1.0.0",
  "license": "GPLV3",
  "repository": "https://github.com/telmotrooper/sleepness",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && electron ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "typescript": "^5.3.3"
  }
}
